# ⚠️ LEGACY BACKUP — NOT PART OF THE LIVE APP
#
# These 9 files are the original pre-backend prototypes (mock data, hardcoded
# "Zaki S." user, `globalResourceProvider` with seed listings, drawer-based
# navigation). They were superseded by:
#   - lib/screens/home_feed_screen.dart  (replaces home_screen + browse_listings)
#   - lib/screens/exchanges_screen.dart  (replaces my_exchanges)
#   - lib/screens/map_screen.dart        (replaces community_map)
#   - lib/widgets/cblrep_shell.dart      (replaces cblrep + app_drawer drawer nav)
#   - lib/screens/create_listing_screen.dart (replaces post_resource_dialog)
#   - lib/providers/listings_provider.dart   (replaces resource_provider)
#   - lib/screens/profile_screen.dart    (replaces dashboard)
#
# Nothing in lib/main.dart (or any live screen/widget/provider) imports this
# folder — verified 2026-09-21. Safe to delete once you confirm the live app
# covers everything you need.
